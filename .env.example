# AROS local environment overrides (copy to .env)
# All values are optional; the defaults live in config/settings.yaml

# Override the database connection URL, e.g. sqlite:///data/aros.db
AROS_DATABASE_URL=

# Override the log level (DEBUG, INFO, WARNING, ERROR)
AROS_LOG_LEVEL=
