"""Core foundation modules for AROS.

This package holds the cross-cutting building blocks that every later sprint
depends on: configuration loading, logging, the database engine, and the
shared exception hierarchy.
"""
